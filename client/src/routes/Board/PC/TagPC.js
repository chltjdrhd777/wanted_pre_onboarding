import React from "react";
import styled, { css } from "styled-components";

function TagPC({ tags }) {
  return <TagLayoutMedia>TagPC</TagLayoutMedia>;
}

const S = {
  TagLayout: styled.div``,
};

const TagLayoutMedia = styled(S.TagLayout)``;
export default TagPC;
