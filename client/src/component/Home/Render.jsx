import React from "react";
import styled, { css } from "styled-components";

function Main({ objData, stringData }) {
  return <S.HomeMain>this is render Container </S.HomeMain>;
}

const S = {
  HomeMain: styled.main``,
};

export default Main;
